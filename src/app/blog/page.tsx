import React from 'react'

function BlogPage({children}:{children: React.ReactNode}) {
    return (
        <div>
            BlogPage
            {children}
        </div>
    )
}

export default BlogPage